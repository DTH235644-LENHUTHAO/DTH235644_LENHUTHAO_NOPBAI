Trình bày ý nghĩa toán tử /, //, %, **, and, or, is:

Toán tử / được dùng để chia số thực. Kết quả của phép chia này luôn là số thực (kiểu float), kể cả khi chia hết. 
Ví dụ: 7 / 2 = 3.5, 8 / 2 = 4.0.

Toán tử // là phép chia lấy phần nguyên. Kết quả của phép chia này là số nguyên nếu cả hai toán hạng là số nguyên, 
hoặc số thực nếu có ít nhất một số thực. Lưu ý, phép chia này luôn làm tròn xuống. 
Ví dụ: 7 // 2 = 3, còn -7 // 2 = -4.

Toán tử % là phép lấy dư. Nó cho ra phần dư của phép chia theo công thức a % b = a - (a // b) * b. 
Ví dụ: 7 % 2 = 1, -7 % 2 = 1.

Toán tử ** là phép lũy thừa. Nó dùng để tính số mũ. 
Ví dụ: 2 ** 3 = 8, hoặc 9 ** 0.5 = 3.0 (căn bậc hai của 9).

Toán tử and là phép logic “VÀ”. Nó trả về True nếu cả hai vế đều đúng. 
Trong Python, and trả về toán hạng cuối cùng được đánh giá. 
Ví dụ: True and False cho kết quả False, 5 and 10 cho kết quả 10, còn 0 and 10 cho kết quả 0.

Toán tử or là phép logic “HOẶC”. Nó trả về True nếu ít nhất một vế đúng. 
Python sẽ trả về toán hạng đầu tiên được coi là đúng, nếu không có thì trả về toán hạng cuối cùng. 
Ví dụ: True or False cho kết quả True, 0 or 10 cho kết quả 10, "" or "Hello" cho kết quả "Hello".

Toán tử is dùng để so sánh hai biến có tham chiếu tới cùng một đối tượng trong bộ nhớ hay không. 
Nếu cùng một đối tượng thì trả về True, ngược lại trả về False. 
Ví dụ: nếu a và b cùng trỏ tới một danh sách [1, 2, 3] thì a is b sẽ là True. 
